# Example Secret — apply once per namespace; replace token in production.
apiVersion: v1
kind: Secret
metadata:
  name: ai-search-assistant-internal
type: Opaque
stringData:
  token: "change-me-internal-token"
